export enum ETokenType {
    REFRESH = 'R',
    PWD_RESET = 'P',
}

export enum EAuthProvider {
  INTERNAL = 'I',
  GOOGLE = 'G',
  APPLE = 'A',
  FACEBOOK = 'F',
}

export enum EOtpType {
  VERIFY = 'V',
  LOGIN = 'L',
  PWD_RESET = 'P',
}

export enum EChannel {
  EMAIL = 'E',
  PHONE = 'P'
}